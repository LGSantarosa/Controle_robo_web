# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtualenv
.venv/
venv/

# Logs
*.log
controle_web/logs/

# Mapas gerados pelo SLAM
maps/

# Colcon workspace (build artifacts deste repo)
build/
install/
log/

# Workspace de build alternativo: src/robot_nav é SYMLINK pra este repo e
# src/wheel_msgs é cópia (com .git próprio) do que já está em ros2_packages/.
# Nada ali é fonte — é scaffolding do colcon.
.ros2_ws/

# PlatformIO build artifacts (firmware/mega_bridge)
.pio/
firmware/**/.pio/

# Editor/SO
*.swp
.DS_Store
.vscode/
.idea/

# Vídeos POV da câmera (grandes — ficam fora do git)
pov_*.mkv
pov_*.mjpeg

# relatório solto na raiz (dono mandou ignorar 07-16)
Relat*rio rob* educa*.pdf

# Driver do LiDAR: clone raso do upstream, com .git próprio dentro. Quem versiona
# é o setup_pi.sh (clona em :119-129 e reaplica o patch do pthread.h em :132-138).
# Sem esta linha, um `git add -A` registra um gitlink vazio em vez do conteúdo.
/ros2_packages/ldlidar_stl_ros2/
